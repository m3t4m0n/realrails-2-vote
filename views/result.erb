<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>투표 결과</title>
    </head>
    <body>
        <h1>결과보기 페이지</h1>
        <% TIMES.each do |id, text| %>
            <%= text %> : <%= @votes[id] || 0 %><br />
        <% end %>
        
    </body>
</html>